# Reference material, not part of the application
DocumentacionLeodega-main/
PrototipoLeodega-main/

# Local agent/tooling artifacts
.atl/

dev.sh
# Local dev helper scripts (not shared)
dev.sh

# Local env files (belt-and-suspenders; subproject .gitignores already cover these)
.env
*.env
.env.local
backend/seed-demo.php
